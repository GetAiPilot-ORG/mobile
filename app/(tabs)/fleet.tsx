import React from 'react';
import { View } from 'react-native';

// Placeholder screen for the "fleet" route used in the bottom tab navigator.
// It does not render any UI and is only required so that Expo Router can resolve the route name.
export default function FleetScreen() {
  return <View />;
}
